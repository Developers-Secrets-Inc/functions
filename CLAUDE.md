When you want to access your memory, run `npx tsx tools/index.ts getMemory` to retrieve your memory. Same, when you want to add a new memory, run `npx tsx tools/index.ts addMemory "Your memory here"`.

You can use `npx tsx tools/index.ts help` to view informations about all tools.